# R8 R6 state decision — 2026-08-16

**Decision:** terminal finding for the current shadow-evidence objective.

Run 5713 has independently adopted proof that the attested R8 source can open
its evidence tail, emit one read-only `ready:true` snapshot, and make that
snapshot retrievable without source sync or core-MCP changes. The evidence
objective is complete.

R8 remains **canary-only**. There is no authorization for a continuous
monitor, score-driven MCP change, game-capital deployment, or production
integration. A future R8 package must begin with a concrete hypothesis that
needs a new bounded observation; it must receive its own named tier gate and
evidence contract. The active controller successor is the independent
`core-pool-not-idle` diagnosis package.
