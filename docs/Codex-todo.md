# Codex current work ledger

This is the concise current Codex list. `docs/claude-todo.md` is preserved as
Claude/history evidence and is not the authority for current permission.

## Now

- [x] Complete the independent project-wide governance audit, run the auditor
  fixture/full-project checks, record its `REVISE` decision, and provisionally
  adopt its tiered control spine plus Standing Orders amendment. Reassess after
  observed use: controls must enable progress, not become a permission queue.
- [ ] Keep the established core MCP baseline available; do not bundle a held
  subsystem into `startup.js`.
- [ ] Keep stock, IPvGO, Darknet, faction share, and further R8 live execution
  off until their narrow promotion-state conditions are cleared.

## Next safe production work after governance review

1. Separate governance, R8, Darknet/share, IPvGO, and quarantined stock work at
   commit/deploy boundaries; do not demand a clean tree for analysis.
2. For R8, first implement/test explicit tail opening in a non-synced worktree
   and retrieve/attest the already-produced cumulative text file. Do not run a
   thirteenth experiment to discover the same evidence-channel defect.
3. Reconcile current subsystem plans to the machine promotion state. Historical
   architecture and scorecards remain useful evidence, not launch authority.
4. Resume the highest-value MCP work that passes its scoped tier gate.

## Standing operating facts

- `/Users/Shared/BitBurner` is the connector-synced checkout.
- A `WATCHED_FILES` source edit is deployment-capable; local implementation
  belongs in a non-synced worktree.
- Tier 0 work never requires Ken's approval.
- Every narrow block must return a correction, owner, clearing evidence, and
  parallel safe work.
