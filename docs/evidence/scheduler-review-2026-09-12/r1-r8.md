# Scheduler R1–R8 tracked review — 2026-09-12

Scope: current source inspection plus historical evidence explicitly dated below. Parent review reports fresh live H275, XP objective, target phantasy, with money-based R8 vetoing an XP-qualified foodnstuff switch at money-score ratio 0.109. Earlier H4271/QLink cash figures are not current observations. No scheduler changes or game actions performed by this review.

| Item and purpose | Code and decision inputs | Enabled state | Test/live evidence and measured benefit | Limitation | Rollback and next action |
|---|---|---|---|---|---|
| R1: match hacking drain to growth recovery | `mcp_logic.js:94 computeWorkWeights`; `mcp.js:962 buildPlan`; hack percent, growth logarithm, money readiness, safety, objective, security/time ratios | Money balance policy active in money mode, safety 0.5; XP uses a separate fixed split | Pure tests in `mcp_logic.test.js`; historical Aug14 live validation in `docs/hacking-strategy.md:846`; combined R1/R4 income improvement approximately $170–190K/s to $11.4M/s, not an isolated R1 effect | Continuous average model plus per-host integer rounding can yield zero hacks; readiness throttles do not appear in potential score; startup/full-call latency remains material | Restore prior tested source/config and restart; next quantify planned versus completed action rates and integer rounding before retuning safety |
| R2: do not call normal floor security a stuck target | `mcp_logic.js:758 evaluateStuckTarget`; security, best security, elapsed progress time, action duration, plan/work margin, thresholds | Unconditional detector; configured timeout 60s | Pure regression tests; Aug13 historical live confirmation (`hacking-strategy.md:833`) | Depends on sampled progress and action-time estimates; old evidence is not proof of current generation behavior | Source rollback/restart; next correlate any eviction with security trajectory and actual action completion |
| R3: redeploy on quantitative allocation mismatch | `mcp_logic.js:535 computeDesiredAllocation`, `:701 hostNeedsRedeploy`; desired/running counts, RAM, weaken budget, tolerance, elapsed process duration | Active, tolerances 2 threads and 20% | Pure regressions; Aug13 live confirmation; later Aug18 missing-action repair showed 99.8% initial RAM use (`STATE.md`) | Process age is not the age of the currently executing call in indefinitely looping workers; per-host allocation also fragments small hack budgets | Restore tested allocation source/restart; next audit completion-safe redeploy and global rounding together |
| R4: select achievable money rate with preparation discount | `mcp_logic.js:194 computeTargetScore`, `:278 computeTargetEffectiveScore`; `mcp.js:668 getFormulaTargetScores`, `:859 rankTargets`, `:1528` switch measure; max money, hack chance/time/fraction, growth, pool, money, horizon | Money mode; Formulas ranking config=1; switch factor=1.3; horizon=3600s | Pure score/ramp tests; Aug14 live move to $11.4M/s; minimum-security ranking exists in current source | Ramp approximation omits first full grow duration, maintenance weaken capacity, and initial security recovery; productive switches compare RAW score and bypass ramp; fixed hour horizon may not fit remaining QLink runway | Set formula ranking flag 0 to restore old estimate, or restore tested source/config/restart; next compare matched floor/current inputs and practical prep time against observed earnings |
| R5: change only mismatched action scripts | `mcp.js:1043 allocateThreads`; `mcp_logic.js:638 missingActionLaunchPlan`; per-script counts, free RAM, running target/process timing | Active | Pure per-script/missing-action tests; Aug14 historical live confirmation (`hacking-strategy.md:955`) | Preserves unrelated scripts but does not itself know a looping script's current action boundary | Restore tested source/restart; next verify interrupted versus completed grow/weaken/hack calls around each redeploy |
| R6: select and allocate for hacking XP | `mcp_logic.js:229 computeXpTargetScore`, `:94 computeWorkWeights`; `mcp.js:764 getTargetXpScore`; base security, chance, current hack time, 95/5 hack/grow weights | Fresh parent observation: XP mode active; current source includes dedicated XP selector | Pure XP ranking tests and historical 190/190 suite; old docs explicitly lacked live XP rate validation; fresh parent observation now confirms branch is exercised, but useful marginal XP gain remains unmeasured | R8 still applies MONEY comparison to XP-qualified switches; fresh live ratio 0.109 demonstrates actual blockage. Current-security XP ranking also undervalues unprepared servers | Durable objective command can return to money; next make money veto inapplicable to XP, browser-test it, then compare live XP per second over several full action cycles |
| R7: recover small capacity/efficiency losses | `mcp.js:915` excludes home; `:1732` growth security callback; `mcp_logic.js:535` allocation; home reserve, grow clamp, security cap | Security cap=1 and grow clamp active; original home-worker addition is NOW DISABLED intentionally | Historical Aug14 home use 97.7%, but obsolete for today's policy; injected grow clamp has pure tests | Do not cite old home utilization as current benefit; work-phase maintenance remains a conservative linear estimate; home has separate control/automation uses | Restore individual tested config/source; next evaluate explicit bounded home allocation only if useful, without enabling restricted share automation |
| R8: block an already-qualified money switch when candidate floor-score ratio is below 0.8 | `mcp_logic.js:394 evaluateFormulaSwitchVeto`; `mcp.js:1549`; enabled flag, selected current/candidate, minimum-security effective money scores, threshold | Config=1; incorrectly invoked in XP too | Pure veto tests; STATE Sep7 records veto at 0.79943 and permissive path at 0.80511; historical benefit is avoiding a modeled inferior money switch, not measured incremental income | It is NOT a selector; invalid data fails open. Cross-objective veto now has fresh live evidence. `hacking-strategy.md:1058` statement that it never fired is stale | Flag 0 is reversible rollback; next restrict evaluation to money objective and prove XP candidate cannot be blocked on dollar score |

## Multi-target conclusion

Worth re-evaluating with fresh capacity and target inputs; not ready to launch existing `mcpMulti.js` unchanged. Its `getWorkerHosts` at line 360 never enumerates cloud servers and still includes home; its line 293 model reads current security, unlike current single-target Formulas ranking. `mcpMulti_logic.js:119 partitionHostsAcrossTargets` greedily fills whole hosts to a 90% saturation threshold, rather than comparing marginal benefit per unit of RAM. A large 4TB host cannot be divided across targets in that implementation. It recomputes assignments every tick without the current scheduler's commitment policy, exposing useful calls to reassignment churn.

Old `docs/hacking-strategy.md:711` estimates (+17% two targets, +22% three) were for a historical 1764GB pool and cannot answer the present question. Additional RAM can make splitting more valuable in the model, but XP is largely per-thread rate rather than money-pool saturation: splitting identical workers across weaker XP targets has no inherent XP advantage.

Next: use the finite read-only `target_probe.js` to compare min-security scores, practical readiness and a coarse two-target model on CURRENT state; first correct observed allocation/objective failures, then browser-test a two-target policy only if modeled benefit exceeds preparation and switching cost. Do not buy capacity based solely on model projections.
